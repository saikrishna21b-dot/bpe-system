server:
  port: 8888

spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: file:///S:/work_space/bpe-config-repo
          clone-on-start: true
          default-label: main
