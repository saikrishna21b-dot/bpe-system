spring:
  application:
    name: gateway-service   # must match YAML file name in config-repo

  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

# optional but recommended
management:
  endpoints:
    web:
      exposure:
        include: "*"
